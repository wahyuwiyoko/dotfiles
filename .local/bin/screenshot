#!/bin/sh

output="$HOME/pictures/$(date +'%Y-%m-%d-%H:%M:%S')-screenshot.png"

case "$1" in
  "select") scrot "$output" -s -l mode=edge || exit ;;
  "window") scrot "$output" -u -b || exit ;;
  *) scrot "$output" || exit ;;
esac

notify-send "Screenshot saved to ~/pictures"
